/**
 * @fileoverview Various ether constants needed for operation.
 */

const consts = (module.exports = {});

/** @const {number} DEFAULT_GAS_BUFFER Gas buffer to add to max gas. */
consts.DEFAULT_GAS_BUFFER = 30000;
